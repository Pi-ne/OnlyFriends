<view class="page auth">
  <view class="brand">Onlyfriends</view>
  <view class="subtitle">发现身边有趣的活动</view>
  <view class="form card">
    <button class="btn wechat block" loading="{{loading}}" disabled="{{loading}}" bindtap="wechatLogin">
      {{loading ? "登录中" : "微信一键登录"}}
    </button>
  </view>
</view>
